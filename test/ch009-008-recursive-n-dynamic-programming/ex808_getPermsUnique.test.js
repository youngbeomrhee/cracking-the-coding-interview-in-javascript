import {
    getPermsUnique,
    printPerms,
} from '../../src/ch009-008-recursive-n-dynamic-programming/ex808_getPermsUnique'

test('getPermsUnique', () => {
    const result = getPermsUnique('abac')
    console.log(result)
})
test('printPerms', () => {
    const result = printPerms('abac')
    console.log(result)
})
